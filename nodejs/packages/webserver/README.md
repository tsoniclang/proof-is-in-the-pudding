# Node HTTP Server

Import-addressable `node:http` proof with a dynamically configurable port and exact response contract.

```sh
npm run build
PROOF_PORT=8765 dotnet run --project out/csharp/ProofNodeWebServer.csproj
```
