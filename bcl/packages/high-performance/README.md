# Span and Memory

Pure CLR proof for `Span<int>`, `Memory<int>`, slicing, fill, array-backed views, and closed bulk copy. No runtime reflection or dynamic dispatch is used.

```sh
npm run build
dotnet run --project out/csharp/ProofBclHighPerformance.csproj
```
