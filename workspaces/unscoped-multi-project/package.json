{
  "name": "proof-workspaces-unscoped-multi-project",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w acme-domain build && npm run -w acme-api build",
    "typecheck": "npm run -w acme-domain typecheck && npm run -w acme-api typecheck"
  },
  "devDependencies": {
    "@tsonic/core": "10.0.41",
    "@tsonic/dotnet": "10.0.41",
    "@tsonic/globals": "10.0.42",
    "tsonic": "0.0.76",
    "typescript": "^5.0.0"
  }
}
