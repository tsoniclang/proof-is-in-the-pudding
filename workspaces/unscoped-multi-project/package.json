{
  "name": "proof-workspaces-unscoped-multi-project",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w acme-domain build && npm run -w acme-domain bindings && npm run -w acme-api build",
    "typecheck": "npm run -w acme-domain typecheck && npm run -w acme-domain build && npm run -w acme-domain bindings && npm run -w acme-api typecheck"
  },
  "devDependencies": {
    "@tsonic/core": "^0.6.2",
    "@tsonic/dotnet": "^0.8.9",
    "@tsonic/globals": "^0.3.9",
    "@tsonic/tsbindgen": "^0.7.20",
    "tsonic": "^0.0.15",
    "typescript": "^5.0.0"
  }
}

