{
  "name": "proof-workspaces-unscoped-multi-project",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w acme-domain build && npm run -w acme-api build",
    "typecheck": "npm run -w acme-domain typecheck && npm run -w acme-domain build && npm run -w acme-api typecheck"
  },
  "devDependencies": {
    "@tsonic/core": "10.0.16",
    "@tsonic/dotnet": "10.0.16",
    "@tsonic/globals": "10.0.16",
    "tsonic": "0.0.47",
    "typescript": "^5.0.0"
  }
}
