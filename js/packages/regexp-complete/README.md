# Complete JavaScript RegExp

Executable JavaScript-surface proof for modern ECMAScript regular-expression grammar, UTF-16 matching, stateful execution, result objects, iteration, replacement callbacks, string protocols, and `RegExp.escape`.

```sh
npm run build
dotnet run --project out/csharp/ProofJsRegExpComplete.csproj
```
