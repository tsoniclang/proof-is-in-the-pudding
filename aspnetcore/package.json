{
  "name": "proof-aspnetcore",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -ws --if-present run build",
    "typecheck": "npm -ws --if-present run typecheck"
  },
  "devDependencies": {
    "@tsonic/aspnetcore": "10.0.1",
    "@tsonic/core": "10.0.1",
    "@tsonic/efcore-sqlite": "10.0.1",
    "@tsonic/globals": "10.0.1",
    "tsonic": "0.0.30",
    "typescript": "^5.0.0"
  }
}
